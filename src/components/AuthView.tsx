// Replaced by the dedicated /login route (src/app/login/page.tsx) in the routing
// + multi-provider auth update. Kept as an empty module because the file can't
// be removed from this environment.
export {};
