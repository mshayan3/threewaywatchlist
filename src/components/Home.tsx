// Retired: the personalized "Recommended" deck that used to be the signed-in
// home. The home page ("/") is now a public discover surface (DiscoverHome),
// and the recommendation engine lives on the Catch-up page (CatchUp.tsx).
// Kept as an empty module because files can't be removed in this environment.
export {};
